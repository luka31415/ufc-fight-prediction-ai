[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"

[project]
name="ufc-ai"
version="0.1.0"
requires-python=">=3.10"
dependencies=["pandas","numpy","scikit-learn","catboost","joblib","requests","beautifulsoup4"]

[tool.setuptools.packages.find]
where=["src"]
